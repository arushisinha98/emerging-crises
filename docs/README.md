## Code Documentation

This directory contains documentation on the code generated for this project. It is structured to represent the packaged code in `src`. Each python file in `src` is accompanied by a subsequent documentation file with the same name but with the .md extension that describes the purpose of the code, the contents of the code file, how to use the classes and functions, relevant examples, and best practices for usage.
